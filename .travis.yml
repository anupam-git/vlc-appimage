language: cpp
compiler: gcc
sudo: require
dist: xenial

before_install:
  - echo "BEFORE_INSTALL"

install:  
  - echo "INSTALL"

script:
  - echo "SCRIPT"
  - build.sh

after_success:
  - echo "AFTER SUCCESS"
